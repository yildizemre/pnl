# Sunucuda: cp deploy/.env.example backend/.env
JWT_SECRET=uzun-rastgele-bir-anahtar-degistir
# SQLite (varsayılan) — dosya backend/data/hypevision.db
# DATABASE_URL=sqlite:////root/hp-pan/backend/data/hypevision.db
# PostgreSQL için:
# DATABASE_URL=postgresql://user:pass@127.0.0.1:5432/hypevision
WS_PUSH_SECONDS=8
